/**
 * Free Upload Manager — Konfiguration
 * =====================================
 * WORKER_URL pekar nu på Oracle Cloud skiten för uppladdning.
 * Cloudflare Workern används bara för download-sidan (/page/).
 */
const FUM_CONFIG = {
  WORKER_URL: 'https://proxy.freeuploadmanager.org',
};
